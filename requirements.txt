# LatMaz NeurIPS 2026 E&D — Python dependencies
#
# Tested with Python 3.11 in a conda env named latMaz_RL. The full
# replication recipe in README.md §1 sets up the env and installs these.
#
# Versions pinned to the ones used to produce the headline numbers in
# Table 1 and the rerun-452 cohort. Looser pins (e.g., torch>=2.0) are
# fine in practice but were not exercised seed-for-seed.

# ---- core RL stack ----
stable-baselines3==2.3.2
sb3-contrib==2.3.0
gymnasium==0.29.1
torch>=2.0,<3.0

# ---- numerics / data / plotting ----
numpy>=1.24,<2.0
pandas>=2.0,<3.0
scipy>=1.10
matplotlib>=3.7
networkx>=3.0
seaborn>=0.12

# ---- POMCP / planners ----
pomdp-py==1.3.5

# ---- Croissant metadata validation ----
mlcroissant>=1.0

# ---- mouse-data preprocessing utilities ----
opencv-python-headless>=4.8  # raw-session frame I/O (optional for code-only replication)

# ---- notebook + figure tooling (optional) ----
jupyter>=1.0
ipython>=8.0
